<div class="card">
    <div class="card-block">
        {{ form_start(form) }}
            <div class="form-group row">
                {{ form_label(
                    form.url,
                    null,
                    {'label_attr': {'class': 'col-sm-2 form-control-label'}})
                }}

                <div class="col-sm-10">
                    {{ form_widget(form.url, {'attr': {'class': 'form-control'}}) }}
                    {{ form_help(form.url) }}
                    {{ form_errors(form.url) }}
                </div>
            </div>

            <div class="form-group row">
                {{ form_label(
                    form.language,
                    null,
                    {'label_attr': {'class': 'col-sm-2 form-control-label'}})
                }}

                <div class="col-sm-10">
                    {{ form_widget(form.language, {'attr': {'class': 'form-control'}}) }}
                    {{ form_help(form.language) }}
                    {{ form_errors(form.language) }}
                </div>
            </div>

            <div class="form-group row">
                {{ form_label(
                    form.pub,
                    null,
                    {'label_attr': {'class': 'col-sm-2 form-control-label'}})
                }}

                <div class="col-sm-10">
                    {{ form_widget(form.pub) }}
                    {{ form_help(form.pub) }}
                    {{ form_errors(form.pub) }}
                </div>
            </div>

            <div class="form-group row">
                {{ form_label(
                    form.isTemplate,
                    null,
                    {'label_attr': {'class': 'col-sm-2 form-control-label'}})
                }}

                <div class="col-sm-10">
                    {{ form_widget(form.isTemplate) }}
                    {{ form_help(form.isTemplate) }}
                    {{ form_errors(form.isTemplate) }}
                </div>
            </div>

            <div class="form-group row">
                {{ form_label(
                    form.isRegexp,
                    null,
                    {'label_attr': {'class': 'col-sm-2 form-control-label'}})
                }}

                <div class="col-sm-10">
                    {{ form_widget(form.isRegexp) }}
                    {{ form_help(form.isRegexp) }}
                    {{ form_errors(form.isRegexp) }}
                </div>
            </div>

            <div class="form-group row">
                {{ form_label(
                    form.title,
                    null,
                    {'label_attr': {'class': 'col-sm-2 form-control-label'}})
                }}

                <div class="col-sm-10">
                    {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
                    {{ form_help(form.title) }}
                    {{ form_errors(form.title) }}
                </div>
            </div>

            <div class="form-group row">
                {{ form_label(
                    form.description,
                    null,
                    {'label_attr': {'class': 'col-sm-2 form-control-label'}})
                }}

                <div class="col-sm-10">
                    {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
                    {{ form_help(form.description) }}
                    {{ form_errors(form.description) }}
                </div>
            </div>

            <div class="form-group row">
                {{ form_label(
                    form.keyWords,
                    null,
                    {'label_attr': {'class': 'col-sm-2 form-control-label'}})
                }}

                <div class="col-sm-10">
                    {{ form_widget(form.keyWords, {'attr': {'class': 'form-control'}}) }}
                    {{ form_help(form.keyWords) }}
                    {{ form_errors(form.keyWords) }}
                </div>
            </div>

            <table>
                <tr>
                    <td>
                        <a class="btn btn-primary btn-sm" href="{{ path('meta_data_index') }}">back to list</a>
                    </td>
                    <td>
                        <button class="btn btn-primary btn-sm">{{ button_label|default('Save') }}</button>
                    </td>
                </tr>
            </table>
        {{ form_end(form) }}

        {% if meta_data.id %}
            {{ include('backend/meta_data/_delete_form.html.twig') }}
        {% endif %}
    </div>
</div>
