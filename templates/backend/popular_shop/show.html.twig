{% extends "backend/base.html.twig" %}

{% block moduleTitle %}Популярные магазины{% endblock %}
{% block moduleSubTitle %}Просмотр{% endblock %}

{% block content %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ popular_shop.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ popular_shop.title }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ popular_shop.url }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ popular_shop.image }}</td>
            </tr>
            <tr>
                <th>Pub</th>
                <td>{{ popular_shop.pub ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('popular_shop_index') }}">back to list</a>

    <a href="{{ path('popular_shop_edit', {'id': popular_shop.id}) }}">edit</a>

    {{ include('backend/popular_shop/_delete_form.html.twig') }}
{% endblock %}
