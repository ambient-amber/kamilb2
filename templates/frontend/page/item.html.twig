{% extends "frontend/base.html.twig" %}

{% block title %}
    {{ page.title }}
{% endblock %}

{% block body %}
    <main>
        <div class="page_content_block">
            <div class="page_content_block_inner">
                <h1>{{ page.title }}</h1>

                {{ page.content|raw }}
            </div>
        </div>
    </main>
{% endblock %}
